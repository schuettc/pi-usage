{
  "name": "pi-extensions",
  "version": "0.10.0",
  "private": true,
  "packageManager": "npm@11.16.0",
  "description": "Monorepo for @sreetej510 pi extension packages published to npm and GitHub Packages.",
  "type": "module",
  "workspaces": [
    "extensions/*"
  ],
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome lint .",
    "biome:check": "biome check .",
    "build": "node ./scripts/build-all.mjs",
    "typecheck": "npm --workspaces run typecheck",
    "test": "npm --workspaces --if-present run test",
    "check": "npm run biome:check && npm run typecheck && npm run build && npm test",
    "pack:hpc-tools": "npm --workspace @sreetej510/pi-hpc-tools pack --dry-run",
    "pack:prompt-manager": "npm --workspace @sreetej510/pi-prompt-manager pack --dry-run",
    "pack:usage": "npm --workspace @schuettc/pi-usage pack --dry-run",
    "pack:shipd-checks": "npm --workspace @sreetej510/pi-shipd-checks pack --dry-run",
    "publish:dry": "npm --workspaces publish --dry-run --access public",
    "publish:github:dry": "npm --workspaces publish --dry-run --access public --registry https://npm.pkg.github.com",
    "release:patch": "node ./scripts/bump-shared-version.mjs patch",
    "release:minor": "node ./scripts/bump-shared-version.mjs minor",
    "release:major": "node ./scripts/bump-shared-version.mjs major"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@earendil-works/pi-tui": "^0.84.2",
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "typebox": "^1.3.3",
    "typescript": "^5.7.3"
  }
}
